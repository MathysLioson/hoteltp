class HotelManagerService {
public:
    HotelManagerService() = default;  // Constructeur par défaut
    ~HotelManagerService() = default; // Destructeur par défaut

    // Ajoute ici les méthodes qui seront nécessaires pour la gestion de l'hôtel
};

int main() {
    // Utilisation de la classe HotelManagerService
    HotelManagerService service;
    // Les appels de méthode sur service iraient ici

    return 0;
}
